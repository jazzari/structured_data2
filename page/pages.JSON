<script type="application/ld+json">
  {
    "@context": "https://schema.org",
    "@graph": [
      {
        "@type": "WebPage",
        "name": "<%= @resource.title %>",
        "datePublished": "<%= @resource.publish_at %>",
        "dateModified": "<%= @resource.updated_at %>",
        "description": "<%= @resource.body %>"
      }
    }
  ]
}
  </script>
  // Maybe here we don't want datePublished or dateModified?
  // You could merge the pull request, then create a branch and change it back...
  